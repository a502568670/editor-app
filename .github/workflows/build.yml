# This workflow is superseded by build-release.yml
# To trigger a build, use build-release.yml instead.
name: '(Disabled - use build-release.yml)'

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Please use build-release.yml."
